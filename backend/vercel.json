{
  "buildCommand": "echo 'Skipping tsc — Vercel compiles api/*.ts and its src/ imports directly, build/ is only used for local/self-hosted runs'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "crons": [
    { "path": "/api/monthlydigest/cron", "schedule": "0 9 1 * *" }
  ]
}
